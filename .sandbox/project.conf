# .sandbox/project.conf — the single fleet pin location. Nothing else may carry a
# golangci-lint version literal; conform's lint-pin rule enforces that.
PROJECT_NAME=upcheck
GOLANGCI_LINT_VERSION=v2.5.0
